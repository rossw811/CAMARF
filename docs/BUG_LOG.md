# CAMARF Bug Log — Index

Quick-navigation index into `Development.md`'s bug registry (proposed Session 26, built Session 28,
2026-07-11, per the second-pass staleness triage's #1 actionable item). **This is a pure INDEX, not a
content move** — every entry below is a one-line summary with a `Development.md:LINE` pointer to the
full write-up. Nothing was deleted or moved out of `Development.md`; that file remains the canonical,
complete record. This file exists only so a ~60-entry bug can be found without reading 10,900+ lines
of session narrative to locate it.

To use: find the bug number or keyword below, then open `Development.md` at the given line for the
full root-cause/fix/verification write-up.

## data.py / fetch pipeline (BUG-D)

| # | Summary | Development.md |
|---|---------|-----------------|
| D01 | Futures ambiguous contract — qualifyContracts returns all expiry months | :147 |
| D02 | Forex contract format — wrong pair ordering in ib.Forex() | :149 |
| D03 | OOM on 1m reindex — date_range allocates before size check | :151 |
| D04 | Holiday gap detection using pandas 'B' misses NYSE holidays | :153 |
| D05 | Warning 2110 infinite reconnect — IBKR upstream broken | :155 |
| D06 | yfinance 1m period "7d" exceeds Yahoo's 8-day hard limit | :157 |
| D07 | ADJUSTED_LAST fails weekly/monthly intervals | :159 |
| D08 | MultiIndex group_by="ticker" breaks extraction | :161 |
| D09 | ProgressLogger PermissionError on OneDrive | :163 |
| D10 | Cache backfill returning session-only keys, missed 5,752 disk files | :165 |
| D11 | 7D/1M yfinance weekly inconsistency across asset classes | :167 |
| D12 | period kwarg missing from get_equity_history() | :169 |
| D13 | Completed assets bypass freshness check | :171 |
| D14 | Excluded assets could still enter backfill | :173 |
| D15 | Cache deletion via rename fails on Windows/OneDrive | :1482 |
| D16 | ETF class not in yfinance routing filter | :1804 |
| D17 | 2m/3m derived from corrupted 1m data | :1812 |
| D18 | Batch yfinance saves 0/N bars | :1852 |
| D19 | IBKR session-dead reconnection infinite loop | :1856 |
| D20 | Config hash forces full re-fetch on every config change | :1860 |
| D21 | analysis.py running full IBKR fetch (architecture violation) | :1864 |
| D22 | 8h batch yfinance saves 0/N (persistent) — led to 8h removal | :2249 |
| D23 | 1h cache contaminated with 8h-frequency data (961 assets) | :2253 |
| D24 | 4h derivation produces silent wrong-frequency output | :2510 |
| D25 | RunSummary table empty despite Phase 2A running | :2523 |
| D26 | 2m systematic 100% fetch failure — period interpretation | :2532 |
| D27 | Explicit-date day-count keyed by wrong variable (introduced by D26's wrong-hypothesis-2) | :2547 |
| D28 | yf.download() unreliable for bulk sequential calls vs. yf.Ticker().history() | :2558 |
| D29 | Stale "45d_fallback" string reaching the API as a literal period | :2569 |
| D30 | Missing pytz dependency in the trading conda environment | :2581 |
| D31 | CRITICAL — fresh yf.Ticker() per call triggers Yahoo anti-bot throttling (root cause of 1h/2m/1m failure pattern) | :2587 |
| D33 | _fetch_constituents_cached unconditionally cached empty live-fetch results | :3171 |
| D34 | pd.read_html(resp.text) no longer accepts literal HTML strings on pandas 2.2.3/3.0.3 (universe collapsed to 86 assets) | :3226 |
| D35 | seed_sp_caches.py picked Wikipedia's historical "Selected changes" table instead of current constituents | :3239 |
| D36 | MIN_BARS_REQUIRED['1m']/['3m'] were mathematically impossible to satisfy | :3255 |
| D37 | 4h resample used clock-aligned bins, not session-aligned (contradicts D32's supposed fix) | :3283 |
| D38 | Stale per-ticker yf_period cache entries with no expiry, silently capping ~80 tickers' 1h fetches | :3296 |
| D39 | SEVERE — analysis.py was never actually read-only (Architecture Rule #1 violation) | :3311 |
| D40 | Cache-contamination frequency checks included overnight/weekend gaps, deleting good 4h files | :3345 |
| D41 | Stale 30m cache from an older pipeline version, missing the morning session | :3366 |
| D42 | Supersedes D31's throttling guess — 1m/2m/3m real root cause | :3734 |
| D43 | _write_analysis_summary() crashed on Optional[float]=None hurst_rs | :3818 |
| D44 | ml.py's confirmed-pair discovery — two bugs, same session | :3854 |
| D45 | SpreadModel.fit_pair's rolling z-score/half-life computed on padded, not real, bars | :3911 |
| D46 | Overnight is_fresh() fix was incomplete — one more call site | :4251 |
| D47 | Confirmed-pair counts in latest_run_analysis.log overstated by 2x+ | :4283 |
| D48 | confirmed_pairs_manifest.json only ever accumulated, never pruned | :4329 |
| D49 | Degenerate, repeated-price bars (price degeneracy) — root-caused to market cap, ~32% of 1m universe. Multiple entries as the investigation progressed | :4568, :4832, :5048, :5137, :6006 |
| D50 | CFTC COT API — wrong dataset ID + wrong contract names + wrong URL construction | :6274 |
| D51 | _clean_close() returns np.ndarray, not pd.Series | :6434 |
| D52 | FDR_ALPHA misconfigured — 0.01 too strict for 65k-pair universe, killed valid 1h pairs | :7603 |
| D53 | Permutation test's trade-level shuffle destroyed genuine cross-pair exit-timing correlation, inflating the null | :9600 |
| D54 | research/decoupling_analysis.py — zero GapFlag masking + live/stale directory double-counting | :10073 |
| D55 | stats.py confirmatory validation stack loads unmasked spread_series_*.parquet | :10112 |
| D56 | backtest.py continuous-forecast-scaling STORM flags silently override coint_frac_sizing instead of composing — fixed 2026-07-11 | :10191 (found), :10958 (fixed) |
| D57 | DataCleaner._standardize() strips tz before snap_timestamps() ever sees it — exchange-aware `.L`/`.T`/`.HK` session handling was dead code in real production flow. FIXED — turned out narrower than expected (DataCleaner.clean() itself needed no changes); snap_timestamps() now handles naive-but-already-local input. VOD.L retention 44.5%→88.9%, 7267.T 57.1%→85.9%. Exercised end-to-end through the real production fetch→clean→snap→cache-write path (2026-07-12) — real cache files written and round-trip confirmed for VOD.L/7267.T/0700.HK | :11076 (found), :11472 (fixed) |
| D58 | survivorship_exclusions.csv conflates "removed from S&P 500 index" with "delisted/no longer trading" — silently truncated 7/24 confirmed 1h pairs (DD/NOV/FHN legs) to zero backtest data. FIXED — `resolve_survivorship_oos_end()` only truncates if real data doesn't extend >90 days past the claimed removal date. IS Sharpe 5.17→5.80, OOS 4.39→5.22, all 24 pairs now included | :11322 (found), :11432 (fixed) |
| D59 | distance.py's cointegration portfolio Sharpe was an unweighted mean of noisy per-pair Sharpes (one 6-day thin pair showed Sharpe=114, dragging the mean to 20.435) instead of pooled daily P&L like the distance method uses. FIXED — real pooled figure is 8.542, genuinely comparable to distance's 7.865 | :11378 |
| D60 | No portfolio-level capital constraint anywhere in backtest.py — hub_weights is opt-in (not used in headline runs), MAX_CONCENTRATION_PCT/ACCOUNT_SIZES are defined in config.py but never read anywhere. Peak 27 concurrent positions, $421,252 real notional at worst moment — headline Sharpe assumes capital never validated against any account size. Found, quantified, addressed via new portfolio_sim.py (capital-constrained + mark-to-market replay, all 3 account tiers) — not wired into backtest.py's own production path, Ross's call whether to | :11562 |
| D61 | distance.py's GGR comparison never tested the same date window on both sides — cointegration side used backtest.py's 20% holdout (~7mo), distance side used its own hardcoded 50/50 formation/trading split (~18mo). FIXED — aligned to the same HOLDOUT_PCT convention. Corrected comparison: coint 8.542 (222 trades) vs distance ~28 (only 16 trades/7 days, bootstrap-unstable, direction real but magnitude not estimable). Residual per-pair-vs-global cutoff gap measured directly (max 5 days, real data; 7 days, synthetic stress case) — trivial, no further code change needed | :11848 |

| D62 | portfolio_sim.py's portfolio_sharpe_from_replay() pooled daily P&L via groupby(exit_date) (drops zero-P&L calendar days), while aggregate_portfolio() — behind every headline Sharpe — uses resample("1D") (zero-fills them). This alone reproduced ~9.79 of the previously-reported "10.21 capital-constrained" figure with ZERO capital constraint applied — the whole "capital constraints raise Sharpe" finding was this convention mismatch, not a real effect. A real but small secondary FIFO variance-suppression effect exists on top (98th percentile vs. random-subsample null; DD/MLI hub-leg signal-storm trades disproportionately skipped). FIXED — function now matches aggregate_portfolio()'s convention exactly; $1M tier (capital never binding) reproduces the 5.8044 headline to 4 decimals, confirming the fix | :12092 (root-caused), :12234 (fix applied) |
| D63 | output/results/confirmed_pairs_manifest.json (live production artifact) contained 6 leftover test-placeholder symbols (AAA/BBB/EEE/FFF/GGG/HHH, each tagged "__TEST_SAVE__") — the exact contamination class PAPER.md §9's AI-disclosure Example #3 describes having fixed once already, recurred because the manifest path had no test/production override at all (root cause: `_save_tf_results`'s manifest path was hardcoded, so the earlier per-script backup/restore fix never generalized to a second script touching the same function). Immediate cleanup: 6 junk keys removed (48→42 real entries). Structural fix: `manifest_path_override` parameter added to `_save_tf_results` (analysis.py), defaults to the real production path (zero changes at the one real call site), overridable by test code. `debug/_verify_save_tf_results_return.py` and `debug/_verify_manifest_pruning.py` both updated to use it — the latter's old backup/restore pattern removed entirely, no longer needed. Both scripts re-run for real, both confirm the real production manifest byte-for-byte untouched | Development.md 2026-07-13 entries (same-day, "test-placeholder symbol contamination" + structural-fix follow-up) |
| D64 | sensitivity.py's `_portfolio_sharpe()` had the identical bug BUG-D62 already found and fixed in portfolio_sim.py — `groupby(exit_date)` pooling (drops zero-P&L calendar days) instead of `resample("1D")` (zero-fills them), never applied here when D62 was fixed elsewhere. Found as a byproduct of task #20's risk-management comparison-arm work. Directly affects PAPER.md §7.8's entry/exit-z grid, ADV sweep, and HL-ceiling sweep — all computed via this function, all potentially inflated. FIXED — function rewritten to match `aggregate_portfolio()`'s convention exactly. Verified on a gap fixture: old convention gave Sharpe 18.71 (2 days, gap-dropped), corrected convention gives 5.95 (11 days, zero-filled) — same signature and magnitude of inflation as D62. §7.8's sensitivity grids need re-running with the fix (tracked under Phase 13's existing checklist item, not yet done) | Development.md 2026-07-13 (same-day entry, "sensitivity.py Sharpe-convention bug") |
| D65 | `DataStore.append()` (data.py) concatenated freshly-fetched, currently-adjusted OHLC bars onto previously-cached bars from an earlier fetch with zero reconciliation when a stock split/reverse-split occurred between the two fetches — confirmed on DD (real 2.390x spinoff adjustment 2025-11-03, real 1-for-3 reverse split 2026-06-24) via a fresh yfinance pull spanning the real split boundary (0 jumps) vs. the contaminated cache (~3x jump at the append seam). FIXED at the root — `DataStore._reconcile_split_adjustment()` added, called from inside `append()`, covers all 13 existing call sites with one change. Rescales using the empirically observed seam ratio (not the raw recorded split factor — its multiply/divide convention proved inconsistent across DD's two real actions), validated against `yf.Ticker(symbol).splits` before touching anything; declines and logs a warning if no matching split explains the gap. `output/cache/DD_1hr.parquet`/`DD_4hr.parquet` were briefly trimmed in place by the fixing agent, beyond its authorized scope (the brief asked for a code fix, not a cache rewrite) — caught, verified against the pre-fix backup, and reverted the same session on Ross's explicit direction: the legacy 2023 stale fragment and the 6 short-window intraday caches' live discontinuities are both left for natural self-heal on the next `data.py` refresh now that `append()` itself is fixed, not a manual rewrite | Development.md 2026-07-13, "BUG-D65: DD cache split-adjustment-seam contamination" |
Development.md — flagging as a numbering note, not filling in a fabricated entry.)*
| D66 | Universe-wide scan (task #51, read-only, `research/data_contamination_scan.py`) found BUG-D65's append-seam mechanism is not DD-isolated: 6 more confirmed-pair symbols (APP, CRWD, MLI, MTZ, VRT, WCC) show the same credible intraday append-seam signature, all 14 events clustered 2023-07-26 to 2023-08-10 — the same 15-day window as DD's own case, strongly suggesting a shared artifact from this project's initial 1h/4h historical backfill rather than 7 coincidental real splits. NOT independently confirmed per-symbol against live yfinance the way DD's case was (that's the natural next step) — a well-grounded pattern-match, not yet individually verified. Separately: the scan's own naive >15%-jump threshold badly over-flags long-horizon timeframes (1mo/3mo/6mo/7day/1day), where legitimate single-stock volatility (earnings gaps, etc.) routinely exceeds 15% — 93% of the raw 248,064 "unexplained" events are in that bucket and are NOT reliable contamination evidence; a real, disclosed limitation of this scan's classifier (no per-symbol news/earnings context), not a universe-wide crisis. No fix needed beyond BUG-D65's existing `_reconcile_split_adjustment()`, which already covers all 13 call sites and self-heals any of these symbols on their next append; no cache files touched (read-only task) | Development.md 2026-07-13, "BUG-D66 / Task #51: universe-wide data-contamination scan" |
| D67 | `near_miss_lag_scan.py`'s output paths for monthly timeframes ("1M", "3M") collide with the minute timeframes ("1m", "3m") on Windows' case-insensitive filesystem — `near_miss_lag_scan_1M.parquet` is confirmed BY CONTENT to actually be the 1-minute result (n_at_best_lag values only plausible for minute bars, not 134 years of monthly bars). `data.py`'s own cache already avoids this via `_1mo`/`_3mo`/`_6mo` suffixes; this script never followed that convention. Real consequence: 1M/3M were silently never computed despite a plausible-looking file existing at each name. Fix in progress: rename to the established `_1mo`/`_3mo`/`_6mo` pattern and genuinely re-run those two timeframes | Development.md 2026-07-14, "BUG-D67: near_miss_lag_scan.py's 1M/3M output paths collide with 1m/3m on Windows" |
| D68 | `pit_wfa.py`'s catastrophic point-in-time result (zero pair overlap, OOS Sharpe -1.04 to -0.72) root-caused: 95.6% (43/45) of its point-in-time confirmed pairs only cleared screening via `passes_coint_frac_secondary_evidence()`'s override (median coint_fraction_rolling 0.129, far below the 0.40 primary threshold) — the override's "no structural break detected" signal is unreliable with a short training window since Zivot-Andrews/CUSUM need real sample size to have power. FIXED — `AnalysisPipeline._MIN_BARS_FOR_SECONDARY_EVIDENCE = 756` (3x the 252-bar rolling window) gates the override, ineligible below that regardless of signal cleanliness. `debug/_verify_coint_frac_override.py` updated + 4 new synthetic cases isolating the gate, 8/8 (well, all non-skipped) pass. NOT yet re-tested against pit_wfa.py itself — deliberately deferred to avoid concurrent heavy compute with the BUG-D65/D66 refetch; two honest possible outcomes on that re-run, not run to confirm a predetermined one | Development.md 2026-07-14, "BUG-D68: pit_wfa.py's catastrophic point-in-time result root-caused..." |
| D69 | `pit_wfa.py`'s `backtest_pair_on_test_window()` built `pair_row` from `full_pair_result` — a SECOND, train+test-recomputed `_build_pair_result()` call (needed for the legitimate per-bar test-window trading series) whose scalar fields (`coint_fraction_rolling`, `half_life_trend_slope`) are NOT point-in-time, silently overwriting the genuinely-causal values `screen_universe_at_cutoff()` already computed and used for the real accept/reject decision. Verified currently inert (no `storm_flags` passed, `RegimeConditioner`/`MLConditioner` both explicitly disabled in this caller, so nothing reads the leaked fields for a live decision) but a fragile invariant, not a guarantee — a future caller enabling those options would silently reintroduce BUG-D68's exact lookahead. FIXED — `pair_row` now overrides those specific fields with the original train-only `pair_result`'s values. Checked `wfa.py` for the same pattern (never re-runs pair selection at all, so it doesn't apply there) | Development.md 2026-07-14, "BUG-D69: pit_wfa.py's test-window backtest built pair_row from a train+test-recomputed pair_result..." |
| D70 | BUG-D62/D64's Sharpe-pooling `groupby(exit_date)`-drops-zero-days bug recurs in 4 more files found via a full-codebase Grand Sweep: `deflated_sharpe.py` (feeds headline DSR), `stats.py`'s permutation test, `cvar.py` (VaR/CVaR), `fresh_holdout_compare.py` (docstring falsely claimed already-correct). FIXED — new shared `portfolio_math.py` module (`daily_pnl_from_trades()`/`sharpe_from_daily_pnl()`), all 4 sites migrated. Real-data delta on cached OOS trades: sr_hat 0.542→0.329, T 91→213, DSR 0.0020→0.0000 (both already near-zero, conclusion unchanged, corrected number is honestly lower). Deliberately NOT migrated: `stats.py::_build_daily_pnl()`, `research/portfolio_effective_bets.py`, `research/return_smoothing_audit.py` — structurally different per-pair-panel/autocorrelation use cases needing their own review | Development.md 2026-07-20, "BUG-D70: BUG-D62/D64's Sharpe-pooling-convention bug recurs in four more files" |
| D71 | `wfa.py` hardcoded its own copy of Config.BACKTEST's strategy constants with a comment claiming they matched — 3 had silently drifted (EXIT_ZSCORE 0.5 vs 0.0, SLIPPAGE_BPS 2.0 vs 5, MAX_HOLD_MULT 3.0 vs MAX_HOLD_MULTIPLIER's 2.0). WFA Sharpe figures already in CLAUDE.md/PAPER.md were computed under a different strategy than backtest.py's own headline run, despite being presented as a robustness check of the same one. FIXED — wfa.py now imports all 7 constants directly from Config.BACKTEST instead of duplicating. `debug/_verify_wfa_config_consistency.py` added as a regression guard. wfa.py's own cached output is now stale relative to the fix, needs a re-run (folds into Phase 13) | Development.md 2026-07-20, "BUG-D71: wfa.py's strategy constants silently drifted from Config.BACKTEST" |
| D72 | `run_storm_grid.py`'s `portfolio_sharpe()` — 7th recurrence of the BUG-D62/D64/D70/D71 Sharpe-pooling class (`groupby("date")` instead of zero-filled `resample("1D")`), inflating every Sharpe in the 16-combination STORM factor grid. FIXED — migrated to `portfolio_math.sharpe_from_trades()`. `debug/_verify_run_storm_grid_sharpe_fix.py` added, same gappy-fixture template as D70, passes. Cached STORM-grid output now stale, needs a re-run (folds into Phase 13) | Development.md 2026-07-20, "BUG-D72: run_storm_grid.py — 7th recurrence of the Sharpe-pooling-convention bug class" |
| D73 | `data_ibkr.py`'s `merge_with_yfinance()` — BUG-D65's split-adjustment-seam bug recurring in a 14th, independent merge path never covered by the original fix's 13 call sites. Corrupts exactly the survivorship-bias/episodic-cointegration analysis this file exists for. FIXED — reuses `DataStore._reconcile_split_adjustment()` on the IBKR (older-basis) side before concat. `debug/_verify_ibkr_merge_split_reconciliation.py` added (synthetic 3-for-1 split fixture + negative control), passes. Not yet re-triggered against real IBKR data (manual/separate pipeline) | Development.md 2026-07-20, "BUG-D73: data_ibkr.py's merge_with_yfinance() — BUG-D65's split-seam bug in an uncovered merge path" |
| D74 | `ml.py`'s `hedge_ratio_drift` and `backtest.py`'s Layer-2 `_ml_hedge_drift` both used the static full-sample `hedge_ratio_ols`/`hedge_ratio_kalman_mean` scalar fields instead of the already-fixed point-in-time `hedge_ratio_ols_t`/`hedge_ratio_kalman_t` series `analysis.py` persists specifically to avoid this — same defect class independently found in `research/comomentum.py`. Currently inert in production (LAYER2_ENABLED=False) but fixed anyway per explicit direction (a gated-off bug is still a bug). FIXED — both now compute the drift fresh per entry event/bar from the PIT series, falling back to the scalar only when `_t` columns are absent. `debug/_verify_ml_hedge_ratio_drift_pit.py` added, confirms drift varies across events instead of being constant; passes | Development.md 2026-07-20, "BUG-D74: ml.py/backtest.py Layer-2 hedge_ratio_drift used the static scalar, not the point-in-time series" |
| D75 | `ml.py::_train_and_validate()`'s median imputation was computed over the FULL chronological dataset (train+val+test) before the split, leaking val/test feature distribution into the value used to fill training-set NaNs. FIXED — median now fit on the train split only, applied to all splits. `debug/_verify_ml_median_imputation_no_leakage.py` added (sparse-train/dense-val-test fixture: OLD median 100.0 vs NEW train-only median 1.0), passes. Not yet re-run against real ML training data | Development.md 2026-07-20, "BUG-D75: ml.py's median imputation computed over the full dataset before the train/val/test split" |
| D76 | **Highest-stakes finding of the Grand Sweep.** `backtest.py`'s risk-parity/HRP/pnl-cap sizing weights defaulted to fitting on the full-series `trades_layer1.parquet`, which necessarily overlaps the trailing-20% window a `--holdout` run evaluates as OOS — real in-sample circularity directly touching the "recommended production" risk-parity Sharpe (5.8689). FIXED — `BacktestEngine.run()` gained an `is_only` mode (exact chronological complement of `holdout_only`); the per-pair loop was extracted into `_run_all_pairs()`; the three weight-fitting functions gained an in-memory `trades_df` override; `main()` now runs a preliminary flat-sizing IS-only fitting pass before the real `--holdout` run when a sizing flag is active. A real-data test run also surfaced and fixed a crash (empty `trades_df` guard missing) via `--tf 1h`'s current 0-confirmed-pairs state. `debug/_verify_backtest_risk_parity_is_only_fit.py` added — confirms non-overlapping IS-only/holdout windows, correctly-swapped weights under swapped volatility profiles, and graceful empty-trades handling. Real-data before/after Sharpe delta not yet possible given the current thin pair-set; historical 5.8689 figure should not be treated as current truth until re-run | Development.md 2026-07-20, "BUG-D76: backtest.py's risk-parity/HRP/pnl-cap sizing weights fit on a window overlapping their own OOS evaluation" |
| D77 | Corrected, narrower version of the reverted EG gap-contamination fix. Built `data.py::is_genuine_data_gap()`/`longest_gap_respecting_segment()` — a per-timeframe hours-based ceiling (120h, calibrated from the real 93h max observed) distinguishing genuine multi-day outages from routine overnight/weekend/holiday closures (both flagged `GapFlag.DATA_GAP` under `align_intraday`'s dense reindex). Wired into `analysis.py`'s `_eg_worker()`/`_rolling_coint_worker()`. FIXED and doubles as task #16 (GapFlag redesign). `debug/_verify_eg_gap_classification_fix.py` added: routine-length synthetic gap bridged identically to the original convention; genuine-length gap with an injected jump correctly NOT bridged, recovering real signal (p=3e-23 vs. the naive p=0.749). Real-data re-check: all 10 currently-relevant pairs match the original convention exactly (confirmed no-op, as predicted) | Development.md 2026-07-20, "BUG-D77: analysis.py's EG gap-contamination — the CORRECTED, narrower fix" |
| D78 | `comomentum.py` reconstructed its entire spread series from a static full-sample `hedge_ratio_ols` scalar — same lookahead class as D74, independently found. FIXED — now loads analysis.py's already-persisted, already-causal `spread` column from `spread_series_*.parquet` directly instead of rebuilding it | Development.md 2026-07-20, "BUG-D78: comomentum.py — same full-sample hedge-ratio lookahead as BUG-D74" |
| D79 | `earnings_lead_lag.py`/`big_move_lead_lag.py` pooled disjoint event windows into one compacted array then ran a positional lagged-correlation shift on it — silently pairs one window's tail with an unrelated, months-distant window's head. FIXED — per-window shift+join, pooling only the resulting value pairs across windows. `debug/_verify_earnings_lead_lag_seam_fix.py` passes (planted lag=+40 artifact: old detects |corr|=0.9999919, fixed shows |corr|=0.39) | Development.md 2026-07-20, "BUG-D79: earnings_lead_lag.py / big_move_lead_lag.py — pooled-window seam contamination" |
| D80 | `follower_direction_validation.py` treated thousands of heavily-overlapping expanding-window "OOS" beta estimates as independent samples in an SEM calculation, manufacturing artificially significant t-stats for almost any pair. FIXED — redesigned onto `comparison_arm_scaffold.walk_forward_windows()`'s genuine non-overlapping train/test splits. `debug/_verify_follower_direction_validation_walkforward_fix.py`: old test rejects the true null 98% of the time; fixed test rejects 2% (nominal 5%) | Development.md 2026-07-20, "BUG-D80: follower_direction_validation.py — manufactured statistical significance" |
| D81 | `jump_diffusion_spread_analysis.py`/`jump_diffusion_parameter_fit.py` dropped DATA_GAP rows THEN diffed — a dropped multi-day gap is indistinguishable from a genuine single-bar jump. FIXED — diff full series first, mask any diff spanning a gap, compact after. `debug/_verify_jump_diffusion_gap_fix.py` passes | Development.md 2026-07-20, "BUG-D81: jump_diffusion_spread_analysis.py / jump_diffusion_parameter_fit.py — drop-then-diff gap contamination" |
| D82 | `predictability_optimizer.py`/`ccp_variants.py` computed a lag-1 cross-covariance via positional `X[1:]`/`X[:-1]` on a dropna'd level series — silently treats rows straddling a dropped gap as one bar apart. FIXED — new `data.py::valid_lag1_mask()` utility excludes gap-spanning transitions from the lag-1 product. `debug/_verify_predictability_optimizer_gap_fix.py` passes | Development.md 2026-07-20, "BUG-D82: predictability_optimizer.py / ccp_variants.py — lag-1 cross-covariance contamination" |
| D83 | `variance_ratio_test.py` — same drop-then-diff class; re-assessed as HIGH-concern mechanism (even routine gaps must not bridge a variance-ratio test). FIXED — new `_longest_clean_run()` restricts to the single longest strictly-contiguous gap-free span before diffing. `debug/_verify_variance_ratio_gap_fix.py`: naive VR(8)=1.0165 (masks mean-reversion), fixed VR(8)=0.7485 | Development.md 2026-07-20, "BUG-D83: variance_ratio_test.py — same drop-then-diff class" |
| D84 | `network_momentum.py` `fillna(0.0)`'d a returns panel with genuine missing-data NaNs, distorting lag-1 cross-correlation for thin/newer symbols. FIXED — NaN preserved throughout; pairwise-complete correlation, NaN-aware weighted signal, no fabricated-zero heuristics needed | Development.md 2026-07-20, "BUG-D84: network_momentum.py — fillna(0.0) distorts lag-1 cross-correlation" |
| D85 | `news_impact_asymmetry.py` borrowed a LEVEL-test's gap-masking justification for its own diff-based, lag-sensitive asymmetry test — doesn't transfer. FIXED — same diff-then-mask reordering as D81 | Development.md 2026-07-20, "BUG-D85: news_impact_asymmetry.py — borrowed gap-masking justification" |
| D86 | `peer_correlation_contamination_check.py` had no gap-flag check on the prior-bar anchor used to compute a same-day return — ironic given the script validates symbols already flagged data-quality-suspect. FIXED — runs through `DataAligner` for a gap_flag column, excludes DATA_GAP-flagged same-day/prior bars | Development.md 2026-07-20, "BUG-D86: peer_correlation_contamination_check.py — no gap-flag check" |
| D87 | `dd_hub_effective_bets.py` dropped each pair's DATA_GAP rows independently before a cross-pair intersection — one pair's unrelated gap could silently double a sibling pair's delta span at the same row. FIXED — full calendar index kept per pair (NaN only at that pair's own gaps), per-column `.diff()`, pairwise-complete `.corr()`. `debug/_verify_dd_hub_asymmetric_drop_fix.py`: fixed approach exactly matches ground truth (0.6730 vs 0.6730), old approach off by 0.0017 | Development.md 2026-07-20, "BUG-D87: dd_hub_effective_bets.py — asymmetric per-pair gap-dropping" |
| Tier5 | ~12 recurring D67/A14-class Windows path collisions (raw tf-label in output filenames) across `audit_price_degeneracy.py`, `price_density_screen.py`, `lead_lag_permutation_check.py`, `lag_sweep_validation.py`, `lag_aware_cointegration_discovery.py` (also Tier 2.1: confirmed live wrong-timeframe read bug, fixed), `big_move_lead_lag.py`, `earnings_lead_lag.py`, `leg_level_early_exit.py`, `relational_regime_indicator.py`, `wavelet_hurst_comparison.py`, `graph_clustering.py`, `coint_frac_window_grid.py` (missing tf-suffix entirely, silently overwrote across timeframes). FIXED — all now use `DataStore._TF_SAFE`'s canonical mapping | Development.md 2026-07-20, "Tier 5 fix: recurring D67/A14-class Windows path collisions" |
| D88 | `vol_targeting_and_drawdown_derisking.py`'s `target_vol` was a full-history scalar median — an early trade's size depended on years-later volatility data. FIXED — expanding (causal) median threaded per-bar through `simulate_trades()`. `debug/_verify_vol_targeting_causal_median_fix.py` passes | Development.md 2026-07-20/21, "BUG-D88: vol_targeting_and_drawdown_derisking.py — full-history scalar target_vol" |
| D89 | `backtest.py::_cf_carver`'s forecast-scale factor was computed once from the entire df passed to `run()`, applied as a constant multiplier throughout (comparison-arm-only, not gated behind a production default). FIXED — expanding (causal) per-bar scale-factor array, mirroring `_garch_stop`'s existing causal pattern. `debug/_verify_cf_carver_causal_scale_fix.py`: fixed early-bar value (3.99) vs old full-sample scalar (1.74); expanding value at the final bar converges exactly to the old scalar | Development.md 2026-07-20/21, "BUG-D89: backtest.py::_cf_carver — full-sample forecast-scale factor" |
| D90 | `fill_timing_sensitivity.py`/`breakout_vs_reversion.py`'s headline aggregates averaged per-pair Sharpe-like ratios instead of pooling trades first — BUG-D59-class, recurring an 8th+ time (the same file's own `run_combination_sweep()` already pools correctly). FIXED — both now pool trades across pairs before computing one Sharpe. `breakout_vs_reversion.py` verified end-to-end on real data | Development.md 2026-07-20/21, "BUG-D90: fill_timing_sensitivity.py / breakout_vs_reversion.py — BUG-D59-class recurring twice" |
| D91 | `earnings_structural_break_correlation.py` pooled break events as independent trials despite DD-hub concentration (73.4% of the 1h pool), and tested 3 window sizes with no multiple-comparisons correction. FIXED — added a hub-exclusion robustness rerun + BH-FDR across window sizes. Verified end-to-end on real (thin, current) data | Development.md 2026-07-20/21, "BUG-D91: earnings_structural_break_correlation.py — DD-hub effective-N inflation" |
| D92 | `short_term_factor_alpha.py`'s Monday-dummy signal is identical across every asset on a given day, so its true independent sample size is ~n_days, not n_days×n_assets, but `n_obs` reported the latter. FIXED — added `n_effective_days` diagnostic alongside `n_obs` | Development.md 2026-07-20/21, "BUG-D92: short_term_factor_alpha.py — Monday-dummy cross-sectional duplication inflates n_obs" |
| D93 | `eg_permutation_check.py`'s circular-shift null rolled the RAW (NaN-containing) b array then re-masked, so every permutation draw's overlap N differed from every other draw's AND from the real-data test's fixed-mask N — an apples-to-oranges null. FIXED — compacts to the fixed real-data overlap mask FIRST, rolls only the fully-finite compacted array. `debug/_verify_eg_permutation_mask_consistency_fix.py`: old N varied 420-450 across draws, fixed N constant at 420 (matching real-data test) on every draw | Development.md 2026-07-20/21, "BUG-D93: eg_permutation_check.py — real-vs-null overlap-mask asymmetry" |
| D94 | **Regression, not an original audit item**: BUG-D77 (this session) changed `_eg_worker`'s signature to require `tf_label`, but 4 EXTERNAL consumer scripts (`decoupling_requalification.py`, `stress_test_replication.py`, `bh_vs_by_full_universe.py`, `fdr_method_comparison.py`) were never updated and would crash (`ValueError: not enough values to unpack`) — found while smoke-testing an unrelated Tier 6 fix. FIXED in all 4, verified end-to-end on real data for the two directly runnable ones. **5th recurrence found 2026-07-22**: `analysis.py`'s own `ThresholdCalibrator.pearson_sensitivity()` had the identical 5-vs-6-element task bug, live in production code this time, not an external script — caught from a live crash during the capstone rerun. Fixed and verified the same way | Development.md 2026-07-21/22, "Regression found and fixed while triaging Tier 6" / "BUG-D94-class recurrence #5" |
| D95 | `analysis.py`'s persistence of `all_candidates.parquet`/`spread_series_*.parquet` was nested inside `if discovered_pairs:` (the final post-coint_frac/post-structural set) — when a timeframe's funnel empties to ZERO final confirmed pairs (1h's exact case), NOTHING was persisted, defeating `filter_ablation.py`'s own stated purpose for exactly the most diagnostically-important case. FIXED — moved both persistence steps (which key off `pairs`, the pre-coint_frac/pre-structural EG+FDR+price-degeneracy survivor set) to their own top-level blocks, independent of `discovered_pairs`; `pairs.parquet` itself correctly still only writes when `discovered_pairs` is non-empty. `debug/_verify_bug_d95_persistence_fix.py` passes: synthetic 2-pair scenario matching 1h's real shape (both cut by coint_frac) confirms all_candidates.parquet + both spread_series files now persist while pairs.parquet correctly does not | Development.md 2026-07-21, "BUG-D95 fixed" |
| D96 | `analysis.py:1166`/`:4430` referenced `Config.ANALYSIS.MIN_OVERLAP_BY_TF`/`Config.ANALYSIS.ADV_FILTER_USD` — both attributes actually live on `Config.STATS` (a post-refactor stale reference; `AnalysisConfig` has neither). Both `getattr(..., default)` calls silently fell through to their defaults instead of raising: `_adv_threshold` was always `0.0` (the entire $25M ADV liquidity filter has been a silent no-op in every production run), `_min_overlap` was always a flat 252 for every TF (discarding the per-TF calibration table — 1m/2m/3m should be 1260, 5m/15m 756, 30m 504, 1h 756, 7D 52, 1M 24). FIXED — both changed to `Config.STATS`. Verified directly: `Config.STATS.ADV_FILTER_USD`→25M, `MIN_OVERLAP_BY_TF["1h"]`→756. Grepped all 12 `StatsConfig` attributes across the whole codebase — these were the only 2 stale references (`FDR_ALPHA` was already correctly `Config.STATS` elsewhere). Consequential: every production confirmed-pair count/Sharpe figure to date was generated without the ADV filter active and with the wrong overlap floor — flagged to Ross, full rerun not yet triggered. Does NOT explain the FELE/MAS exclusion under investigation (ADV was off, not on, in the run being investigated) | Development.md 2026-07-21, "BUG-D96: analysis.py — Config.ANALYSIS/Config.STATS stale reference" |
| D97 | `research/audit_price_degeneracy.py`'s output filenames used `safe_tf` (`DataStore._TF_SAFE`-mapped) while `analysis.py`'s `_apply_research_screen_flags()` read the raw `tf_label` — silently broke the audit-to-production handoff for any TF whose file got (re)generated; the working 1m/2m/3m/5m/15m/30m files on disk predated whatever changed this. First fix attempt (switch output to raw tf_label) caused a REAL Windows case-insensitive filename collision (`"1m"`==`"1M"`, `"3m"`==`"3M"`), silently overwriting the correct 1m/3m audit files with 1M/3M data — caught by inspecting file contents (a ~300-row "1m" audit is impossible for real 1-minute data), not assumed clean. FIXED correctly: reverted the writer, instead made the reader use the same `DataStore._TF_SAFE` mapping the writer already used — both sides now agree. Regenerated and verified all 12 TFs; corrupted 1m/3m data fully restored. Verified end-to-end against the real production method with real `PairResult` instances (BFS newly flagged at 1h; KVUE/KMB@2m and 7267.T/8058.T@1M correctly unaffected) | Development.md 2026-07-21, "BUG-D97: research/audit_price_degeneracy.py — tf-label naming mismatch + Windows filename collision" |
| D98 | `analysis.py::_run_one_tf`'s ADV liquidity filter computed its per-symbol proxy exclusively from `{sym}_1hr.parquet`; when that file doesn't exist (161 symbols in the current universe, overwhelmingly international `.HK`/`.T` tickers) it silently returned NaN, and `NaN >= $25M` is `False`, so the symbol was excluded from EVERY timeframe's candidate pool — not because it's illiquid, but from a data-availability gap in the proxy. Surfaced by the BUG-D96 rerun: dropped the real, previously-confirmed 7267.T/8058.T@1M pair (8058.T has no 1hr cache at all). FIXED — `_compute_adv` now falls back to `{sym}_1day.parquet` (one row per day already, no aggregation needed) when 1hr is absent, only NaN if neither exists. Verified synthetically (`debug/_verify_adv_1day_fallback.py`, 6 checks) and directly on real data (7267.T unchanged via 1hr, 8058.T now resolves via 1day to ¥12.5B/day, clears threshold). Known, separate, not fixed here: the proxy does no currency conversion (doesn't change today's outcome for JPY-denominated 7267.T/8058.T, but is a latent gap for other international symbols) | Development.md 2026-07-22, "Capstone rerun #1 — both fixes confirmed active, but surfaced a THIRD bug" |
| Tier6 | Minor findings batch: `lead_lag_scan.py::_eg_pvalue()` bare except (now logs at DEBUG, shared by 7 files), `adf_confirmatory_tier.py` exception text never actually saved (added `adf_status`), `regime_cluster_robustness_check.py`/`rmt_feature_denoising.py` bare excepts in bootstrap loops (now counted/logged), `data_contamination_scan.py` wrong-but-inert manifest schema + cosmetic sign-loss print bug (both fixed), `regime_conditional_analysis.py` docstring overclaimed an unimplemented Welch t-test (doc corrected), `cross_timeframe_divergence.py` usage example showed an unregistered CLI flag (doc corrected), `bias_budget.py` hardcoded stale "378 delist events" (now reads the real file), `decoupling_requalification.py`/`stress_test_replication.py` missing gap-flag masking (now aligned via DataAligner), `filter_ablation.py` crash-vs-zero-trades ambiguity (added explicit status field), `backtest.py`'s `z_rolling != 0.0` filter dropped genuine mid-series zero-crossings (removed, verified via synthetic test), `config.py` dead/aspirational fields (annotated inline, not deleted) + `backtest.py`'s docstring overclaiming an unenforced `MAX_CONCENTRATION_PCT` cap (corrected) | Development.md 2026-07-21, "Tier 6 findings: silent exceptions, doc drift, dead config" |

## analysis.py / architecture (BUG-A)

| # | Summary | Development.md |
|---|---------|-----------------|
| A01 | build_returns_matrix equal-length requirement dropped 98% of universe | :177 |
| A02 | Structural pairs leaking into pairs.parquet | :179 |
| A03 | GOOGL/GOOG not detected as structural (share-class) pair | :181 |
| A04 | HMM convergence warnings fire for any log-likelihood decrease below tol | :183 |
| A05 | OOM guard allocated date_range before checking | :185 |
| A06 | Excluded assets could enter analysis via universe.data | :187 |
| A07 | Hurst estimator wrong domain | :1488 |
| A08 | Nested _fit_one_regime (updated name) | :1808 |
| A09 | INCLUDE_QQQ_EXTRAS/RUSSELL_TOP_N/INCLUDE_BRK_HOLDINGS missing from Config | :1816 |
| A10 | NameError `_canonical_cutoff` not defined | :1868 |
| A11 | _fit_one_regime nested function not picklable | :1872 |
| A12 | `_clean_close` NameError kills all EG testing | :2245 |
| A13 | Bias-relevant — build_returns_matrix() imported but never called _gap_aware_returns | :3332 |
| A14 | output/results/3m/ and output/results/3M/ collide on Windows | :3382 |

## How to add a new bug

When fixing a new bug in `Development.md`, add the write-up there as usual (this file is not where
bug write-ups get authored), then add one row here pointing at it. Keep the summary to one line —
the full context stays in `Development.md`.
