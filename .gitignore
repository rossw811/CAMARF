__pycache__/
*.pyc

# Large generated data/output — regenerate via the pipeline (see README.md
# "Reproducibility" / CLAUDE.md "Data Test Range & Reproducibility") rather
# than tracking multi-GB binary parquet in git history. latest_run_*.log
# summaries at repo root are small, curated, and intentionally still tracked
# (the pattern below matches the raw stdout/stderr dumps, NOT that file).
output/

# graphify-out/: the interactive chart + report ARE tracked (graph.html,
# graph_standalone.html, graph.json, GRAPH_REPORT.md, manifest.json) so the
# knowledge graph is browsable on GitHub without regenerating it locally.
# Only the regenerable AST/semantic cache and machine-local scratch state
# (e.g. .graphify_python, a literal local filesystem path) are ignored.
graphify-out/cache/
graphify-out/.graphify_*

# Fixed 2026-07-11: the old pattern here was `_*.log`, which matches ZERO
# real filenames in this repo (every log is named latest_*.log, not
# _*.log) — a real bug that let 20 raw stdout/stderr dumps get tracked by
# accident. This pattern actually matches them, while NOT matching the
# intentionally-tracked latest_run_*.log summaries (those don't end in
# _stdout.log/_stderr.log).
latest_*_stdout.log
latest_*_stderr.log

# Raw pipeline-runner console output at repo root — same "regenerate, don't
# track" rationale as output/ above. Not a curated summary like
# latest_run_*.log.
pipeline_runner.log
pipeline_runner_out.log

# logs/archive/: ad-hoc raw run logs (underscore-prefixed, e.g.
# _data_ibkr_run.log) that predate the 2026-07-11 latest_*_stdout/stderr.log
# fix above and got tracked by the same bug — same rationale, untracked here.
logs/archive/*.log

# Added 2026-07-15: the project's own scratch-log convention has since
# drifted to `{task}_out.log`/`{task}_err.log` (PowerShell Start-Process
# -RedirectStandardOutput/-RedirectStandardError naming, used throughout the
# 7/14-7/15 overnight session for pit_wfa.py/analysis.py background runs) —
# the *_stdout.log/*_stderr.log pattern above doesn't match this suffix at
# all, so ~30 of these accumulated as untracked cruft requiring manual
# exclusion from every commit this session. Matches at the repo root only
# (doesn't touch anything under output/, already ignored above, or the
# small number of pre-existing tracked exceptions like
# analysis_overnight_err.log/latest_run_data_err.log — gitignore patterns
# never retroactively untrack already-committed files).
/*_out.log
/*_err.log
/*_out[0-9].log
/*_err[0-9].log